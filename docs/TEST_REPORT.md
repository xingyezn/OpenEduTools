# V0.1 测试记录

日期：2026-09-21

## 自动化

- `npm run check`：通过；包含 5 个工具元数据与 Schema 规则校验、索引及 5 个离线包同步、20 项 Node 单元测试、关键相对路径、静态隐私与网络规则。
- `npm run smoke:chrome`：通过；真实 Chrome 无头实例完成首页加载与 5 个工具核心流程。
- `npm run smoke:edge`：通过；真实 Edge 无头实例完成同一组流程。
- 两个浏览器均在 360 CSS px 视口下无页面级横向溢出；CDP 200% 页面缩放下，成绩统计主操作保持可见且无横向溢出。
- 浏览器烟雾测试记录到的运行时错误为 0，HTTP(S) 第三方请求为 0。
- `random-picker.zip` 已真实解压，并在 Chrome 中通过 `file://` 直接完成点名；无控制台错误、无网络请求，也不会显示失效的重复下载入口。

浏览器烟雾测试覆盖：目录加载、介绍字段模糊搜索、学科筛选、5 个离线下载入口、GitHub 入口、随机点名、每组人数分组、1 秒倒计时到时、组合文本清洗、5 个分数的统计与分数段，以及每页横向溢出。

## 交互走查

在 Codex 内置 Chromium 浏览器中完成：

- 首页按描述搜索、清除条件、收藏筛选与最近使用；
- 5 个工具的典型核心流程，控制台 warning/error 为 0；
- 键盘从首页搜索框移动到“清除条件”并触发，从点名名单移动到“随机抽取”并触发；
- 首页、公共页面和 5 个工具在 360×800 视口下检查，均无页面级横向溢出。

## 尚需发布者验证

- Firefox 或 Safari 未安装，未完成第二浏览器引擎验证；Chrome 与 Edge 均为 Chromium 系；
- 未执行真实移动端浏览器或真机测试；
- 200% 使用自动化页面缩放模拟，尚未在桌面浏览器 UI 中手动设置缩放复核；
- 无头浏览器使用静音参数，随机点名的提示音可听性仍需人工复核；
- 尚未由两名独立测试者完成无口头指导的可用性测试；
- GitHub Pages 已启用并可通过 `https://xingyezn.github.io/OpenEduTools/` 访问；每次发布后的线上缓存刷新和下载文件响应仍需复核。

完整人工清单见 [RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md)。
