# 为 OpenEduTools 做贡献

感谢你帮助教师减少重复、琐碎的工作。OpenEduTools 欢迎代码、文档、设计、测试、需求反馈，以及借助 AI 完成并经过人工验证的贡献。

提交前请先阅读 [TOOL_SPEC.md](TOOL_SPEC.md)。本文件说明协作流程；`TOOL_SPEC.md` 是工具实现和验收的规范来源。

## 行为原则

- 尊重不同地区、学段、学科和技术背景的贡献者。
- 讨论问题本身，给出可复现的信息和建设性建议。
- 不在 Issue、示例、截图、测试文件或提交记录中放入真实学生信息。
- 不提交无权使用的教材、试题、图片、字体、代码或其他受版权保护内容。

## 可以贡献什么

### 报告问题

请说明：

1. 使用的工具和页面地址；
2. 预期行为与实际行为；
3. 可复现步骤；
4. 浏览器、操作系统和设备；
5. 必要时提供已脱敏的截图或示例数据。

### 提议新工具

先创建 Issue，至少说明：

- 谁会使用它；
- 它解决的单一核心问题；
- 典型输入与输出；
- 是否能完全在浏览器本地运行；
- 是否处理名单、成绩等敏感数据；
- 与现有工具是否重叠；
- 最小可用版本包含什么、不包含什么。

### 改进现有内容

小型拼写、样式或无障碍修复可以直接提交 PR。影响交互、元数据结构、公共组件、分类或隐私边界的改动，请先创建 Issue。

## 开发流程

1. Fork 仓库并从默认分支创建短生命周期分支。
2. 阅读相关 Issue、`TOOL_SPEC.md` 和附近已有实现。
3. 只完成一个清晰主题，避免顺手重构无关文件。
4. 本地预览并完成手工测试。
5. 运行仓库提供的校验脚本。
6. 使用清晰的提交信息，推送分支并创建 Pull Request。

推荐分支名：

```text
feat/random-picker
fix/mobile-timer-layout
docs/tool-metadata
```

推荐提交信息：

```text
feat: add random picker tool
fix: preserve timer state after pause
docs: clarify local-only data policy
```

## 新增工具

复制 `tools/_template/` 最小工具模板，创建：

```text
tools/<tool-id>/
├── index.html
├── style.css
├── script.js
└── tool.json
```

然后：

1. 使用小写 kebab-case 的稳定 `tool-id`；
2. 完成并校验 `tool.json`；
3. 复用公共设计变量和工具页组件；
4. 确保直接打开 `index.html` 时核心能力可用；
5. 确保所有数据默认在本地处理；
6. 生成或更新 `data/tools.json`；
7. 在桌面和窄屏环境测试；
8. 检查键盘操作、焦点、标签、错误提示和结果朗读；
9. 在 PR 中附上已脱敏截图和测试说明。

维护脚本不依赖第三方包。完成修改后运行：

```bash
npm run build:index
npm run check
```

## 编码约定

### HTML

- 使用语义化元素，如 `header`、`nav`、`main`、`section`、`form`、`footer`。
- 页面必须设置 `lang="zh-CN"`、字符集和 viewport。
- 每个输入控件都要有可见标签；不能只依赖 placeholder。
- 按钮使用 `button`，导航使用链接，不用可点击的 `div` 模拟。
- 结果变化必要时通过 `aria-live` 通知辅助技术。
- DOM 中不得使用内联事件处理器，如 `onclick="..."`。

### CSS

- 优先使用公共设计变量，不复制颜色、间距和圆角常量。
- 工具专属样式只放在本工具的 `style.css`。
- 从窄屏开始设计，避免固定宽度和水平滚动。
- 保留清晰的 `:focus-visible` 状态，并支持 `prefers-reduced-motion`。
- 不用颜色作为唯一状态提示。

### JavaScript

- 使用原生 ES Modules 或带 `defer` 的脚本；避免污染全局作用域。
- 使用 `const`/`let`，不使用 `var`。
- 将解析、计算、格式化等纯逻辑与 DOM 操作分离，便于测试。
- 所有用户输入都要验证；错误信息要说明如何修正。
- 向 DOM 输出用户内容时使用 `textContent`，不要把未经净化的内容传给 `innerHTML`。
- 本地存储键使用 `openEduTools:` 前缀，并处理存储不可用或内容损坏的情况。
- 不使用 `eval`、`new Function`，不动态执行用户输入。

## Vibe Coding 与 AI 辅助贡献

OpenEduTools 鼓励合理使用 Codex 等 AI 编程工具，也欢迎不熟悉前端的教师参与。AI 生成不等于免审查；提交者是最终责任人。

建议流程：

1. 把 Issue、`README.md`、`TOOL_SPEC.md` 和相关文件提供给 AI；
2. 要求 AI 先复述边界和验收标准，再进行小步实现；
3. 检查每个修改，不提交无法解释的代码；
4. 使用虚构、最小化的数据测试，不向 AI 服务提供真实学生资料；
5. 核对第三方依赖、许可证、引用和生成素材来源；
6. 手动验证正常、空输入、错误输入、极端输入、移动端和键盘操作；
7. 在 PR 中说明使用了 AI、人工验证了什么，以及仍存在哪些不确定性。

AI 辅助贡献不得：

- 捏造测试结果、Issue、用户反馈或许可证信息；
- 引入不明来源的代码、图片、字体或大段内容；
- 为完成一个小工具而加入框架、后端、追踪器或远程服务；
- 上传真实名单、成绩、作业或研究数据作为提示词或测试样本；
- 绕过规范、持续集成或评审要求。

仓库根目录的 [CODEX_PROMPT.md](CODEX_PROMPT.md) 可作为 Codex 首次开发任务的起始提示词。

## 隐私与安全检查

提交前确认：

- [ ] 默认不向网络发送用户输入或文件；
- [ ] 没有密钥、令牌、个人信息、真实学生数据或内部地址；
- [ ] 没有分析、广告、指纹识别或追踪代码；
- [ ] 用户输入不会作为 HTML 或脚本执行；
- [ ] 文件类型、大小和解析错误都有合理处理；
- [ ] 导出内容不会意外包含未选择的数据；
- [ ] 清空或重置功能不会误删其他工具的数据；
- [ ] 新增依赖已说明用途、版本、许可证和本地托管方式。

发现安全或隐私问题时，请不要在公开 Issue 中粘贴敏感样本或可直接利用的细节。通过仓库维护者公布的安全联系渠道报告；若尚未配置，请仅提交不含利用细节的公开提醒。

## 测试要求

每个工具至少覆盖：

- 正常输入；
- 空输入；
- 格式错误；
- 重复值、空行、超长文本或大数据量等边界情况；
- 重置、再次运行和导出；
- 刷新页面后的预期状态；
- 键盘完整操作；
- 360px 左右窄屏与常见桌面宽度；
- 最新稳定版 Chrome 或 Edge，以及 Firefox 或 Safari 中至少一种。

如果仓库已经提供自动化测试，新增或修复逻辑时应同步更新测试。自动化测试不能替代手工可用性检查。

## Pull Request 要求

PR 描述应包含：

- 改动目的和关联 Issue；
- 用户可见变化；
- 实现边界和未包含内容；
- 测试环境、步骤和结果；
- 桌面与移动端截图（界面变化时）；
- 隐私、安全、依赖和兼容性影响；
- 是否使用 AI 辅助以及人工复核范围。

建议使用以下检查表：

- [ ] 改动符合 `TOOL_SPEC.md`；
- [ ] 没有引入后端、登录或不必要依赖；
- [ ] 元数据和工具索引已更新；
- [ ] 校验与测试通过；
- [ ] 直接打开单个工具仍可使用核心能力；
- [ ] 响应式、键盘操作和无障碍已检查；
- [ ] 示例和截图不含真实敏感数据；
- [ ] 文档已同步更新。

## 评审标准

维护者主要从以下方面评审：

1. 是否解决明确的教育工作问题；
2. 是否保持小而专一；
3. 是否遵守本地处理和隐私优先；
4. 是否易懂、易用、可访问；
5. 是否能在 GitHub Pages 和相对路径下运行；
6. 是否容易由后续贡献者维护；
7. 是否有充分测试与清晰说明。

评审可能要求缩小范围或拆分 PR。这通常是为了降低长期维护成本，而不是否定需求本身。
