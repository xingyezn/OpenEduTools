# 隐私说明

OpenEduTools 是一个免费、开源、无需登录的教师微工具集合。本说明解释我们如何处理数据，以及匿名使用统计的边界。

## 一句话总结

**你的教学内容永远留在浏览器本地；我们只统计“某个工具被打开或使用了几次”这类匿名计数。**

## 本地处理

以下数据只在你的浏览器内处理，不会发送到 OpenEduTools 或任何服务器：

- 学生姓名、学号、名单；
- 成绩、分数、成绩段；
- 你粘贴或上传的文本、CSV、文件及其文件名；
- 手写数字样本、图片、导出内容；
- 你在工具里填写的任何设置内容（可选的本地偏好除外）。

这些数据存在于页面内存、`localStorage`、`IndexedDB` 或你主动保存的文件中。刷新页面或清除浏览器数据后即消失。

## 匿名使用统计

为了了解哪些工具真正有用，并在首页展示热门工具，OpenEduTools 记录**匿名事件计数**。

### 记录什么

每次事件只包含两个字段：

```json
{ "tool_id": "random-group", "event": "tool_use" }
```

- `tool_id`：工具 ID，例如 `random-group`；
- `event`：事件类型，仅有 `tool_open`、`tool_use`、`favorite_add`、`favorite_remove`、`share`。

服务器按 **UTC 日期 × 工具** 聚合成计数，例如“`random-group` 在某天被使用了 295 次”。

### 不记录什么

统计系统**不会**收集或保存：

- 姓名、邮箱、手机号、账号；
- 学生信息、成绩、名单；
- 你输入或上传的文本、文件、文件名；
- API Key、浏览器指纹、设备唯一 ID、追踪 Cookie；
- 长期保存的 IP 地址或位置信息。

服务器不保存每次访问的原始日志，只保存每日聚合后的数字。

### 如何工作

- 客户端逻辑集中在 `js/analytics.js`，所有工具只能通过它上报。
- `tool_open` 在同一浏览器会话（`sessionStorage`）中，同一工具 30 分钟内只记录一次。
- `tool_use` 在每次明确执行核心功能时记录，不做去重。
- 收藏与分享只在用户主动操作时记录。
- 任何统计请求失败都会被静默忽略，**统计系统故障绝不影响工具使用**。
- 单工具离线包完全不包含统计逻辑，永不联网。

### 数据去向

统计请求发送到 Cloudflare Worker（Cloudflare D1 数据库），每日由 GitHub Actions 导出为公开的 `data/stats.json`，供首页展示使用量、收藏量和热门排行。该文件不包含任何用户信息。

## 如何退出统计

- 统计只在正式站点上运行；本地开发和离线包不会发送任何数据。
- 你可以使用浏览器扩展或网络设置拦截对 `*.workers.dev` 的请求，工具功能不受影响。
- 禁用 JavaScript 可以完全阻止统计，但工具本身也需要 JavaScript 才能运行。

## 第三方

V0.1 不包含广告、第三方分析、第三方追踪器或远程字体。分享功能会打开你选择的目标平台页面，是否分享由你决定。

## 联系

如对本说明或数据处理有疑问，请通过仓库 Issue 或维护者公布的安全渠道反馈。请勿在公开 Issue 中粘贴真实学生数据或敏感样本。
