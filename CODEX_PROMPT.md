# OpenEduTools：给 Codex 的首次开发任务提示词

下面的内容可以直接复制到 Codex，建议在已初始化并包含本仓库文档的项目目录中使用。

---

你正在为开源项目 **OpenEduTools** 实现 V0.1。请在当前仓库中完成一个可部署到 GitHub Pages 的静态教师工具网站。

## 项目目标

OpenEduTools 是免费、开源、无需登录、浏览器本地运行的教师微工具集合。核心原则是：

**Browser First / Local First / No Backend / No Login / Open Source**

技术栈限定为：

- HTML5；
- CSS3；
- Vanilla JavaScript。

不要引入 React、Vue、Angular、Svelte、Astro、jQuery 或必须执行的前端打包链。网站和单个工具应直接以静态文件运行。允许使用 Node.js 维护脚本做元数据校验、索引生成和测试，但用户使用网站时不能依赖 Node.js。

## 权威文档与冲突顺序

开始修改前，请完整阅读：

1. `TOOL_SPEC.md`：工具实现与验收规范；
2. `V0.1_TASKS.md`：范围、任务、依赖和发布门槛；
3. `CONTRIBUTING.md`：协作、隐私、安全与测试要求；
4. `README.md`：产品定位和仓库结构。

如文档与现有代码冲突，先分析冲突并选择最小、最安全的处理；不要静默扩大范围。用户的新明确要求优先，其次是 `TOOL_SPEC.md`、`V0.1_TASKS.md`、`CONTRIBUTING.md`、`README.md`。

## 本次任务范围

按 `V0.1_TASKS.md` 的依赖顺序完成 V0.1，至少包括：

1. 静态站点目录、公共页面和 GitHub Pages 配置；
2. 统一设计变量、公共组件、响应式布局和明暗主题；
3. 每工具独立目录与 `tool.json`；
4. JSON Schema、元数据校验脚本、`data/tools.json` 生成脚本；
5. 首页工具目录、搜索、分类筛选；
6. 基于 `localStorage` 的收藏与最近使用；
7. 统一工具页外壳和贡献者模板；
8. 5 个首批工具：
   - 随机点名；
   - 随机分组；
   - 课堂计时器；
   - 文本清洗；
   - 成绩统计；
9. 对核心纯函数、目录能力和本地状态的测试；
10. 持续集成、Pages 部署、文档同步。

严格遵守 V0.1 的排除项：不实现账号、后端、云同步、分析追踪、广告、联网 AI、多语言、PWA 或插件系统。

## 执行方式

先进行只读检查，再实施修改：

1. 检查当前文件、Git 状态和已有实现，保留用户已有且与任务无关的更改。
2. 对照 `V0.1_TASKS.md` 输出简短实施顺序，并指出已完成、缺失和存在冲突的项目。
3. 从基础设施和公共能力开始，小步实现；不要先复制 5 套重复页面。
4. 先建立元数据 Schema、校验和索引生成，再接入首页目录。
5. 先建立公共工具页样式与共享行为，再实现各工具。
6. 核心算法与 DOM 操作分离，优先写可测试的纯函数。
7. 每完成一个阶段就运行相关校验和测试，并立即修复失败。
8. 最后进行完整回归、隐私检查、相对路径检查和文档核对。

除非遇到会改变产品方向、数据政策或引入第三方依赖的决定，否则请基于文档做合理判断并继续，不要因小型实现细节停下来反复提问。

## 实现约束

- 工具核心能力必须可在直接打开其 `index.html` 时运行。
- 首页可以通过静态服务器读取生成的 `data/tools.json`，但读取失败时要给出友好说明。
- 所有资源和链接使用兼容 GitHub Pages 项目子路径的相对地址。
- `tool.json` 是唯一元数据源；`data/tools.json` 必须生成，不手工维护。
- 用户输入默认只在内存中处理，不持久保存名单、成绩和文本。
- 收藏、最近使用、主题等非敏感偏好使用 `openEduTools:` 前缀。
- 不使用 CDN、远程字体、分析脚本、广告、追踪器或远程 API。
- 不提交任何密钥、真实学生数据、个人信息或不明版权素材。
- 用户输入写入 DOM 时优先用 `textContent`，不得执行动态代码。
- 随机工具不能使用 `sort(() => Math.random() - 0.5)`；按规范使用安全随机与无偏洗牌。
- CSV 处理必须正确处理常见引号和换行规则，不能用单次 `split(',')` 作为完整解析器。
- 界面文案使用简洁中文，面向教师，不堆叠技术术语。
- 确保键盘操作、清晰焦点、可见标签、动态状态通知、颜色对比和减少动画支持。
- 支持约 360px 窄屏、200% 缩放以及主流现代浏览器。

## 5 个工具的最低要求

以 `V0.1_TASKS.md` 的 OET-050 至 OET-054 为权威验收标准。特别注意：

- 随机点名：去空行、可选去重、可移出已抽中人员、重置候选池；
- 随机分组：按组数或每组人数、无偏洗牌、均衡分配、复制结果；
- 课堂计时器：倒计时与正计时、暂停继续、时间戳防漂移、大字展示；
- 文本清洗：操作可组合、默认不做有损转换、差异摘要、复制与下载；
- 成绩统计：分数验证、描述统计、及格率、自定义分数段、正确舍入和安全导出。

## 验证要求

至少完成并报告：

- 元数据 Schema 与所有 `tool.json` 校验；
- `data/tools.json` 生成后无未提交差异；
- 搜索、分类、收藏、最近使用测试；
- 5 个工具的核心算法测试，包含典型、空、错误和边界输入；
- 站内关键相对路径检查；
- 无意外网络请求和控制台错误检查；
- 键盘、约 360px 窄屏、200% 缩放的手工检查；
- 至少两类现代浏览器的冒烟测试；如果当前环境无法执行真实浏览器测试，明确列出未验证项，不得声称已通过。

测试应确定、可复现，不得依赖一次随机结果。不要为了让测试通过而削弱真实功能或校验。

## 完成标准

只有在以下条件满足时才结束：

- V0.1 所有 P0 任务已完成，或明确列出无法完成的外部阻塞；
- 5 个工具都能从首页发现并完成核心流程；
- 网站可作为纯静态内容部署到 GitHub Pages；
- 校验和自动化测试通过；
- 实现与 `README.md`、`CONTRIBUTING.md`、`TOOL_SPEC.md`、`V0.1_TASKS.md` 一致；
- 没有用占位实现、虚假测试结果或未解释的范围删减冒充完成。

## 最终回复格式

完成后请简洁报告：

1. 已交付的用户可见功能；
2. 关键实现与架构决定；
3. 实际运行的校验/测试及结果；
4. 未完成项、已知限制或需要人工验证的内容；
5. 主要修改文件；
6. 本地预览和 GitHub Pages 启用方法。

不要只描述计划；请实际修改文件、运行验证并把仓库推进到可发布状态。
